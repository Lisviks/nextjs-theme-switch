# Theme switch example with css custom properties and scss modules

## Getting started

First install required packages

```bash
npm install
```

Then run the development server:

```bash
npm run dev
```
